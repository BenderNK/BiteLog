//
//  Color.swift
//  BiteLog
//
//  Created by Nessa Kucuk, Turker on 6/15/25.
//

import SwiftUI

extension Color {
    // MARK: - Base Colors
    static let systemRed = Color(UIColor.systemRed)
    static let systemGreen = Color(UIColor.systemGreen)
    static let systemBlue = Color(UIColor.systemBlue)
    static let systemOrange = Color(UIColor.systemOrange)
    static let systemYellow = Color(UIColor.systemYellow)
    static let systemPink = Color(UIColor.systemPink)
    static let systemPurple = Color(UIColor.systemPurple)
    static let systemTeal = Color(UIColor.systemTeal)
    static let systemIndigo = Color(UIColor.systemIndigo)
    static let systemBrown = Color(UIColor.systemBrown)
    static let systemMint = Color(UIColor.systemMint)
    static let systemCyan = Color(UIColor.systemCyan)
    
    // MARK: - Grayscale
    static let systemGray = Color(UIColor.systemGray)
    static let systemGray2 = Color(UIColor.systemGray2)
    static let systemGray3 = Color(UIColor.systemGray3)
    static let systemGray4 = Color(UIColor.systemGray4)
    static let systemGray5 = Color(UIColor.systemGray5)
    static let systemGray6 = Color(UIColor.systemGray6)
    
    // MARK: - Backgrounds
    static let systemBackground = Color(UIColor.systemBackground)
    static let secondarySystemBackground = Color(UIColor.secondarySystemBackground)
    static let tertiarySystemBackground = Color(UIColor.tertiarySystemBackground)
    
    static let systemGroupedBackground = Color(UIColor.systemGroupedBackground)
    static let secondaryGroupedBackground = Color(UIColor.secondarySystemGroupedBackground)
    static let tertiaryGroupedBackground = Color(UIColor.tertiarySystemGroupedBackground)
    
    // MARK: - Labels
    static let label = Color(UIColor.label)
    static let secondaryLabel = Color(UIColor.secondaryLabel)
    static let tertiaryLabel = Color(UIColor.tertiaryLabel)
    static let quaternaryLabel = Color(UIColor.quaternaryLabel)
    
    // MARK: - Fill Colors
    static let systemFill = Color(UIColor.systemFill)
    static let secondarySystemFill = Color(UIColor.secondarySystemFill)
    static let tertiarySystemFill = Color(UIColor.tertiarySystemFill)
    static let quaternarySystemFill = Color(UIColor.quaternarySystemFill)
    
    // MARK: - Text Colors
    static let placeholderText = Color(UIColor.placeholderText)
    static let link = Color(UIColor.link)
    static let separator = Color(UIColor.separator)
    static let opaqueSeparator = Color(UIColor.opaqueSeparator)
    
    // MARK: - Fixed Colors
    static let lightText = Color(UIColor.lightText)
    static let darkText = Color(UIColor.darkText)
    
    // MARK: - Additional iOS 15+ Colors
    static let tintColor = Color(UIColor.tintColor)
    
    // MARK: - Bar Colors
    static let systemBar = Color(uiColor: UIColor.systemBackground)
    static let secondarySystemBar = Color(uiColor: UIColor.secondarySystemBackground)
    
    // MARK: - Button Colors
    static let systemButton = Color(UIColor.systemBlue)
    static let systemButtonDisabled = Color(UIColor.systemGray)
    
    // MARK: - Control Colors
    static let controlBackground = Color(uiColor: UIColor.systemGray6)
    static let controlTint = Color(UIColor.systemBlue)
    static let controlAlternativeTint = Color(UIColor.systemTeal)
    
    // MARK: - Keyboard Colors
    static let keyboardBackground = Color(uiColor: UIColor.systemGray6)
    static let keyboardKey = Color(uiColor: UIColor.systemBackground)
    
    // MARK: - Table Colors
    static let tableCell = Color(uiColor: UIColor.secondarySystemBackground)
    static let tableHeader = Color(uiColor: UIColor.tertiarySystemBackground)
    static let tableSeparator = Color(uiColor: UIColor.separator)
    
    // MARK: - ScrollView Colors
    static let scrollViewBackground = Color(uiColor: UIColor.systemGroupedBackground)
    static let scrollViewIndicator = Color(uiColor: UIColor.systemGray2)
    
    // MARK: - Tab Bar Colors
    static let tabBarBackground = Color(uiColor: UIColor.systemBackground)
    static let tabBarItem = Color(uiColor: UIColor.label)
    static let tabBarSelectedItem = Color(uiColor: UIColor.systemBlue)
    
    // MARK: - Toolbar Colors
    static let toolbarBackground = Color(uiColor: UIColor.systemBackground)
    static let toolbarItem = Color(uiColor: UIColor.label)
    
    // MARK: - Navigation Bar Colors
    static let navBarBackground = Color(uiColor: UIColor.systemBackground)
    static let navBarTitle = Color(uiColor: UIColor.label)
    
    // MARK: - Alert & Action Sheet Colors
    static let alertBackground = Color(uiColor: UIColor.secondarySystemGroupedBackground)
    static let alertButton = Color(UIColor.systemBlue)
    static let destructiveButton = Color(UIColor.systemRed)
    
    // MARK: - System Grouped Colors
    static let groupedContentBackground = Color(uiColor: UIColor.secondarySystemGroupedBackground)
    static let groupedContentSecondaryBackground = Color(uiColor: UIColor.tertiarySystemGroupedBackground)
}
